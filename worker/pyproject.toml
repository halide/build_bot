[project]
name = "worker"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "buildbot-worker~=3.1",
    "cmake>=3.28",
    "ninja>=1.11",
    "pywin32>=311 ; sys_platform == 'win32'", # Missing from buildbot-worker
    "twisted>=23.10",
]
